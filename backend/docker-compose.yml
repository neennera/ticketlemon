version: "3.9"
services:
  ticketlemon_app:
    build: .
    ports:
      - "80:80"
    environment:
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=root
      - DB_NAME=ticketlemon
      - REDIS_HOST=redis
    depends_on:
      - db
      - redis
    volumes:
      - .:/app
  ticketlemon_db:
    image: mysql:8
    restart: always
    environment:
      MYSQL_DATABASE: ticketlemon
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
  ticketlemon_redis:
    image: redis:alpine
    ports:
      - "6379:6379"
